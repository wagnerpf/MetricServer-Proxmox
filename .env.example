# ============================================================================
# CONFIGURAÇÕES DO INFLUXDB
# ============================================================================
# Credenciais iniciais do InfluxDB
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=influxdb_senha_forte_123

# Token do InfluxDB (gerado após inicialização)
# Gere um token com:
#   docker-compose exec influxdb bash
#   influx org create -n proxmox-org
#   influx bucket create -n proxmox-metrics -o proxmox-org -d 30d
#   influx auth create --org proxmox-org --write-buckets proxmox-metrics
# Cole o token gerado aqui:
INFLUXDB_TOKEN=seu-token-gerado-aqui

# ============================================================================
# CONFIGURAÇÕES DO GRAFANA
# ============================================================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=grafana_senha_forte_456

# ============================================================================
# NOTA IMPORTANTE: PROXMOX METRIC SERVER
# ============================================================================
# Proxmox VE envia dados NATIVAMENTE via Metric Server (sem necessidade de API)
# Configure no WebUI do Proxmox:
#   Datacenter > Metric Server > Add
#   Tipo: InfluxDB
#   Host: IP/hostname deste servidor Docker
#   Port: 8086
#   Organization: proxmox-org
#   Bucket: proxmox-metrics
#   Token: Use o token gerado com: docker-compose exec influxdb influx auth create --org proxmox-org --write-buckets proxmox-metrics

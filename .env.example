# ============================================================================
# CONFIGURAÇÕES DO INFLUXDB
# ============================================================================
# Credenciais iniciais do InfluxDB
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=influxdb_senha_forte_123

# Organização e bucket padrão (usados pelo Proxmox Metric Server)
INFLUX_ORG=proxmox-org
INFLUX_BUCKET=proxmox-metrics
INFLUX_RETENTION=30d

# IMPORTANTE: Depois de inicializar, gere um token em:
# docker-compose exec influxdb influx auth create \
#   --org proxmox-org \
#   --write-buckets proxmox-metrics
# Use este token na configuração do Proxmox Metric Server
INFLUXDB_TOKEN=seu-token-gerado-aqui

# ============================================================================
# CONFIGURAÇÕES DO GRAFANA
# ============================================================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=grafana_senha_forte_456

# ============================================================================
# CONFIGURAÇÕES DO PROXMOX VE - IMPORTANTE
# ============================================================================
# NÃO É NECESSÁRIO configurar credenciais aqui com o novo Metric Server
# 
# O Proxmox Metric Server funciona diferente:
# 1. Configure no WebUI do Proxmox: Datacenter > Metric Server > Add
# 2. Aponte para este servidor (InfluxDB)
# 3. Use os dados abaixo quando solicitado no Proxmox WebUI
#
# Valores a usar no Proxmox WebUI:
PROXMOX_METRIC_SERVER_HOST=seu-servidor-docker  # IP ou hostname do servidor Docker
PROXMOX_METRIC_SERVER_PORT=8086                 # Porta do InfluxDB
PROXMOX_METRIC_SERVER_ORG=proxmox-org           # Organização InfluxDB
PROXMOX_METRIC_SERVER_BUCKET=proxmox-metrics    # Bucket InfluxDB
# PROXMOX_METRIC_SERVER_TOKEN=use-token-gerado   # Token do InfluxDB (copie do passo 5)

# ============================================================================
# CONFIGURAÇÕES OPCIONAIS
# ============================================================================
# Node Exporter (coleta métricas do host Docker - opcional)
# NODE_EXPORTER_PORT=9100

# Telegraf (para coletar métricas adicionais do host - opcional)
# TELEGRAF_INTERVAL=10s
